# 정의는 루트 docker-compose.yml 하나다. 이 파일은 실행 위치가 backend/일 때
# (gradlew bootRun, IDE 기본 working directory) spring-boot-docker-compose가
# 루트 정의를 찾게 하는 include 심(shim)이다.
# name은 include로 상속되지 않으므로 루트와 동일하게 고정한다 (같은 compose 프로젝트 공유)
name: meterengine

include:
  - ../docker-compose.yml
