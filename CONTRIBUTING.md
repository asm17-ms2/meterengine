# CONTRIBUTING

브랜치, 커밋, PR 규칙의 정본은 이 파일이다. 규칙이 바뀌면 여기를 먼저 고친다. Notion의 [개발 워크플로 (브랜치/PR 규칙)](https://app.notion.com/p/3ac0899b32b881148d49e3b341bd5034) 페이지는 합의에 이른 논의 기록이다.

## 확정된 규칙

- main에는 직접 push할 수 없다 (GitHub 브랜치 보호로 강제). 모든 변경은 브랜치를 만들어 PR로 올린다
- docs/의 문서도 코드와 같은 PR 흐름을 따른다. 무엇을 두는지는 `docs/document-rules.md` 참조
- work/는 개인 작업 공간이라 PR 대상이 아니다
- 브랜치 네이밍: `<type>/MS2-<이슈번호>-<설명>` 형식. type은 feat, fix, docs, refactor, test, ci, chore 중 하나. 예: `docs/MS2-31-branch-strategy`, `feat/MS2-40-event-ingest`. 브랜치명에 이슈 키가 들어가 Jira가 브랜치/PR을 이슈에 자동 연결한다. 티켓 없이 가는 잡일은 키를 빼고 `<type>/<설명>` 형식으로 쓴다 (아래 "티켓 없이 가는 작업")
- 커밋 메시지: 제목은 `<type>: <요약> (MS2-xxx)`이고 티켓이 없으면 `(MS2-xxx)`를 뺀다. 본문은 왜 / 변경 사항 / 결과와 검증 절로 쓴다. type은 브랜치 네이밍과 같은 목록에서 고른다. 사소한 변경(오타, 링크, 버전 올리기)은 제목 한 줄로 끝내도 된다. 절 제목에 `#`을 쓰지 않는다. 편집기로 커밋하면 git이 `#`으로 시작하는 줄을 주석으로 지운다
- 커밋 개수: PR 하나에 커밋 하나를 최대한 유지한다. squash merge라 어차피 main에는 하나로 남고, 브랜치 안에서 커밋을 나눠도 리뷰가 나아지지 않는다. 나눠야 한다면 PR 본문에 왜 나눴는지 적는다
- 머지 방식: squash merge. PR 하나가 main 커밋 하나로 남아 이슈 단위 추적이 쉽고, 브랜치 안 커밋 정리에 힘 쓰지 않아도 된다. main 룰셋의 Allowed merge methods를 squash만 허용으로 설정해 강제한다. squash로 원본 커밋과의 연결이 끊기는 문제는 아래 "머지 후 브랜치 삭제"와 "선행 브랜치 위 작업" 규칙이 안전장치다
- 고정 브랜치 도입 시 전환 계획: 이후 테스트서버 운영 등으로 develop/staging 같은 고정 브랜치가 생기면, develop 룰셋은 squash만, main 룰셋은 merge commit만 허용으로 전환한다. 오래 사는 브랜치를 main과 반복 머지할 때 squash를 쓰면 히스토리가 꼬이기 때문이다. 시점은 릴리스/태그 규칙 논의와 함께 정한다
- 머지 후 브랜치 삭제: 머지된 브랜치는 삭제하고, 이어지는 작업은 main에서 새 브랜치를 딴다. 다만 아직 머지되지 않은 선행 작업에 의존하면 main이 아니라 그 브랜치 위에 쌓는다(아래 "스택 PR"). GitHub 저장소 설정(Automatically delete head branches)으로 자동화한다. 로컬 브랜치는 각자 `git fetch --prune`으로 정리한다
- PR 리뷰: 작성자 본인 외 1명 이상 승인 후 머지한다. 셀프머지는 하지 않는다. main 룰셋의 Required approvals(1명)로 강제한다
- main 상태: main은 항상 빌드/테스트가 통과하는 상태를 유지한다. 깨진 코드나 반쯤 만든 기능은 브랜치에만 둔다. CI 실패 시 머지를 금지한다. 필수 체크 항목은 CI 워크플로(`.github/workflows/ci.yml`)의 `backend`, `frontend` job이다. main 룰셋의 Required status checks에 그 job을 등록해 강제한다
- 선행 브랜치 위 작업: 브랜치 A가 리뷰 대기 중이고 B가 A 없이는 성립하지 않으면 A 위에 쌓는다. 따로 머지돼도 되면 독립 PR로 낸다. 절차는 아래 "스택 PR"에 있고, GitHub 네이티브 기능만 쓴다
- 릴리스/태그 규칙: 버전 태그는 두지 않는다. 배포 단위는 커밋이고, 배포된 것을 가리키는 이름은 그 커밋의 git SHA다(ECR 이미지 태그가 SHA다). 롤백도 이전 SHA로 다시 배포하는 것이라 semver 태그 없이 성립한다. 외부에 버전 번호를 알려야 하는 일이 생기면 그때 다시 논의한다

## PR 크기와 쪼개기

리뷰가 읽기 편한 단위가 기준이다. 줄 수는 그 단위를 지키게 하는 상한이지 목표가 아니다.

- **하한은 없다.** 1줄 PR도 그게 읽기 편하면 그대로 낸다. 함수명 변경처럼 기계적인 작업도 단일 기능이 아니면 한꺼번에 바꾸지 않고 나눈다
- **상한은 300줄이다.** 추가된 줄(+)을 테스트까지 포함해 센다. 빼고 세는 것은 아래뿐이다
  - 생성물: `backend/openapi.yaml`, 잠금 파일
  - 삭제된 줄
- 삭제를 빼고 세므로 순수 삭제는 사실상 상한을 받지 않는다. 죽은 코드를 지우는 것을 상한으로 막지 않으려는 것이다. 대신 지우는 근거는 PR 본문에 적는다
- 같은 파일을 지우고 다시 쓰는 재작성은 숫자보다 리뷰가 무겁다. 삭제가 추가보다 크면 리뷰어가 지운 것과 새로 쓴 것을 둘 다 읽어야 하므로, 그런 PR은 상한과 별개로 더 잘게 나눈다
- 생성물은 줄 수에서만 빼는 것이고, diff에 올라왔는지는 리뷰에서 본다. CI가 검사하지 않아 빠뜨려도 아무것도 실패하지 않는다 (CLAUDE.md "OpenAPI 생성물")
- **테스트는 기능과 같은 PR에 둔다.** 다음 PR로 미루지 않는다. 테스트 없는 기능 PR은 리뷰어가 맞는지 판단할 근거가 없고, CI도 "기존 테스트가 안 깨졌다"만 확인하게 된다. 테스트만 있는 PR은 이미 머지된 코드의 테스트를 보강할 때로 한정한다
- **초과**: `backend/`, `.github/`, `deploy/`는 초과하지 않는다. `frontend/`, `demo/`는 작성자 판단으로 넘길 수 있고 PR 본문에 이유를 적는다
- **PR 하나당 서브태스크 하나**를 목표로 한다. 맡은 서브태스크가 300줄을 넘길 것 같으면 티켓부터 쪼갠다. 티켓을 새로 만들 필요가 없는 자잘한 작업은 아래 "티켓 없이 가는 작업"을 따른다

### 티켓 없이 가는 작업

오타, 죽은 링크, 포맷, 액션이나 의존성 버전 올리기처럼 자잘한 작업은 Jira 티켓을 만들지 않고 그냥 PR로 올린다. 티켓을 만들고 상태를 옮기는 비용이 작업 자체보다 큰 경우다.

가르는 기준은 나중에 Jira에서 되짚을 일이 있는지다.

- **티켓을 만든다**: 동작이나 정책이 바뀌는 것, 왜 했는지 diff 밖에 설명이 필요한 것, 스프린트 범위나 진행 상황으로 추적해야 하는 것
- **그냥 올린다**: 결과가 diff에 다 드러나고, 무엇을 왜 했는지 커밋 메시지 한 줄로 끝나는 것

애매하면 티켓을 만든다. 티켓 없이 올릴 때도 왜 했는지는 PR 본문에 적는다.

티켓이 없으면 브랜치명에서 이슈 키를 뺀다. `docs/fix-readme-link`처럼 `<type>/<설명>`으로만 쓴다.

### 쪼개는 축

층은 최소 단위가 아니다. 컨트롤러 하나가 엔드포인트 여러 개이고, 통합테스트 파일 하나가 서로 독립된 `@Test` 여러 개다. 위에서부터 적용한다.

1. **동작별** - 엔드포인트나 화면 동작이 여럿이면 그걸로 자른다. 기본 축이다. 각 PR은 동작 하나 + 그 동작이 처음 쓰는 DTO/예외 + 그 동작의 테스트를 갖는다
2. **층별** - 동작이 하나뿐인데 크면 모델 / 서비스 / 컨트롤러로 자른다. 새 도메인 모델을 들여올 때가 이 경우다
3. **경로별** - 그래도 넘으면 성공 경로 / 검증 실패 / 동시성으로 자른다. 통합테스트가 마지막 조각에 몰릴 때 쓴다

**바닥은 관찰 가능한 동작 하나다.** 그 아래로는 쪼개지 않는다. DTO만 있는 PR이나 검증 로직만 떼낸 PR은 호출되는 곳이 없어 리뷰어가 모양이 맞는지 판단할 수 없다.

고객 CRUD(전체 1593줄)를 동작별로 자르면 아래처럼 된다. 테스트를 포함한 값이다.

| 조각 | 줄 |
| --- | --- |
| 엔티티 + 리포지토리 + 스키마 테스트 | 79 |
| GET 목록 + 응답 DTO + 테스트 | 200 |
| POST 등록 + 요청 DTO + 테스트 | 200 |
| PUT 수정 + 테스트 | 180 |
| DELETE + 예외 + 동시성 테스트 | 300 |

한 슬라이스가 여러 조각으로 나뉘면 스택 PR로 쌓는다. 머지 순서는 아래 "스택 PR"에 있다.

## 스택 PR

**독립 PR이 먼저다.** 따로 머지돼도 상관없는 작업이면 각각 main에서 브랜치를 따 독립 PR로 낸다.

스택은 아래 중 하나일 때 쓴다.

- 조각이 따로 머지되면 반만 동작하는 상태가 main에 남는다. 한 덩어리로 갔어야 할 작업이 300줄 상한 때문에 쪼개진 경우가 대부분이다
- 뒤 PR이 앞 PR 없이는 성립하지 않는다. 줄 수와 무관하다
- 한 기능을 순서대로 읽어야 이해된다. 파일 이동과 내용 변경을 나눈 경우처럼, 각 조각은 따로 봐도 말이 되지만 왜 그렇게 했는지는 순서로만 드러난다

세 번째는 "그게 편해서"로 넓어지기 쉽다. 순서를 바꿔 읽어도 이해되면 독립 PR이다.

스택으로 갈 때는 main이 아니라 선행 브랜치 위에 쌓는다. 몇 단이든 쌓아도 된다.

어느 PR에서 머지하든 그 아래 머지되지 않은 PR이 함께 들어간다. 최상단에서 머지하면 슬라이스 전체가 한 번에 올라가고, 하단만 승인됐으면 그것만 단독으로 머지할 수도 있다. 남은 자식들은 base가 자동으로 다시 잡힌다. 스택 안의 각 PR에도 "본인 외 1명 승인"이 그대로 걸리므로, 잘게 쪼갠다고 리뷰가 헐거워지지는 않는다 (2026-08 확인).

### 머지 순서

**호출 경로가 열리는지로 가른다.**

- **내부 구조만 늘리는 조각** (엔티티, 리포지토리, 내부 서비스): 담당자 재량이다. 승인되는 대로 하나씩 머지하든, 몇 개씩 묶든, 최상단에서 한 번에 밀든 상관없다. 아무도 호출하지 않는 코드라 밖에서 보이는 것이 달라지지 않는다
- **컨트롤러나 화면이 들어가는 조각**: 가급적 한 번에 머지한다. 머지되는 순간 밖에서 부를 수 있는데, 뒤에 올 검증이나 오류 처리가 아직 없으면 반만 동작하는 API가 노출된다

어느 쪽이든 각 PR은 자기 브랜치에서 필수 체크를 통과해야 머지되므로 "main은 항상 빌드/테스트가 통과하는 상태"는 유지된다.

**GitHub 네이티브 스택 PR 기능만 쓴다. 수동 git으로 스택을 만들거나 정리하지 않는다.** 2026-08 기준 public preview라 널리 알려진 기능이 아니어서, 익숙한 수동 rebase 절차로 되돌아가기 쉽다. 그러면 스택으로 인식되지 않는 PR이 생기거나 히스토리가 어긋난다.

| 하려는 일 | 쓰지 않는다 | 대신 쓴다 |
| --- | --- | --- |
| 선행 브랜치 위에 새 작업 시작 | `git checkout -b`로 그냥 따기 | `gh stack init` |
| 이미 있는 스택에 합류 | 브랜치 이름으로 checkout | `gh stack checkout <PR번호>` |
| PR 올리기 | `gh pr create --base <부모브랜치>` (diff만 맞고 스택으로 인식되지 않는다) | `gh stack submit` |
| 부모가 갱신됐을 때 맞추기 | `git rebase --onto`, `git rebase --update-refs`, `git rebase -i` | `gh stack sync` |
| 부모가 머지된 뒤 | 손으로 base 바꾸기, 손으로 rebase | 아무것도 하지 않는다. GitHub가 base를 다시 잡는다. 로컬만 `gh stack sync` |
| 스택 브랜치 push | `git push --force` | `gh stack submit` |
| 구조 확인 | `git log --graph`로 추정 | `gh stack view` |
| 이미 올린 PR을 스택으로 묶기 | base만 바꾸기 | `gh stack link` 또는 웹에서 묶기 |

확장이 없으면 `gh extension install github/gh-stack`으로 설치한다. 설치나 명령이 실패하면 수동 git으로 우회하지 말고 멈추고 물어본다. 모르는 하위 명령은 추측하지 말고 `gh stack --help`나 아래 문서를 본다.

- 개념과 머지 동작: https://github.github.com/gh-stack/introduction/overview/
- CLI 레퍼런스: https://github.github.com/gh-stack/reference/cli/
- GitHub 공식 문서: https://docs.github.com/ko/pull-requests/get-started/about-stacked-prs

승인되지 않은 부모를 끌고 들어가는 자식 머지는 요청하지 않는다. 머지는 웹에서 하든 `gh stack merge`로 하든 상관없다. main 룰셋에 bypass가 전부 꺼져 있어(2026-08 확인) 어느 경로로 머지해도 승인과 필수 체크가 똑같이 강제된다.

머지 큐를 켜면 스택의 원자성이 깨진다. 큐 모드는 각 PR을 아래에서부터 개별로 평가해서 main이 중간 상태를 실제로 거친다. 지금은 쓰지 않으며, 도입을 논의할 때 이 항목을 같이 본다.

## 주석과 javadoc

주석을 쓰지 않는다. 코드만 읽어도 이해되게 쓰고, 코드가 맞게 도는지는 테스트로 관리한다.

주석을 쓰고 싶어지면 그 내용은 아래 중 한 곳으로 간다.

| 쓰고 싶은 것 | 가는 곳 |
| --- | --- |
| 왜 이렇게 설계했는지, 무엇을 검토하고 무엇을 택했는지 | 노션 MS2 팀 위키. 정책과 설계 결정의 정본이다 |
| 이 변경을 왜 했는지 | 커밋 메시지와 PR 본문. blame으로 찾아진다 |
| 순진하게 고치면 깨진다, 이 제약을 지켜야 한다 | 테스트. 고치면 실패하고 테스트 이름이 이유를 말한다 |

주석은 틀려도 아무것도 실패하지 않는다. 그래서 시간이 지나면 코드와 어긋난 채 남아 읽는 사람을 잘못된 곳으로 보낸다. 구조로 강제해야 하는 규칙이 생기면 주석이 아니라 아키텍처 테스트나 CI 검사로 넣는다.

이름과 구조로 표현되지 않아 주석을 붙이고 싶어지면, 대개 이름이나 분리가 잘못된 것이다. 주석을 쓰는 대신 그쪽을 고친다.

### 주석이 아닌 것

아래는 이 규칙의 대상이 아니다.

- **도구 지시문**: `@SuppressWarnings`, `// eslint-disable-next-line`처럼 도구가 읽는 것. 설명이 아니라 지시문이다. 억제가 필요한 이유까지 덧붙이지는 않는다
- **`@Schema`, `@Operation`의 description**: 주석이 아니라 API 계약이다. `backend/openapi.yaml`로 나가 프론트엔드와 외부가 읽는다

### Jira 키

Jira 키는 **브랜치명, 커밋 메시지, PR 제목과 본문**에만 쓴다. 레포 안의 파일에는 쓰지 않는다.

이들은 과거의 기록이라 시간이 지나도 참이다. 반면 레포 안의 파일은 지금 상태를 서술하므로, 외부 시스템의 상태를 박아 넣으면 그 티켓이 움직일 때 문서만 조용히 틀려진다. 주석을 쓰지 않는 이유와 같다.

브랜치명의 키는 Jira 자동 연결에 필요해 생략할 수 없다. 티켓 없이 가는 작업은 위 "티켓 없이 가는 작업"에 따라 뺀다.

`@Schema`와 `@Operation`의 description에는 특히 넣지 않는다. `backend/openapi.yaml`로 생성되어 프론트엔드와 외부가 읽으므로, 내부 티켓 키가 그대로 나간다.

키를 지우는 것으로 끝내지 않는다. "단가 등록은 MS2-xxx 예정이다" 같은 문장은 "단가 등록은 아직 없다"로 다시 쓴다. 읽는 사람이 알아야 할 것은 지금 없다는 사실이지 티켓 번호가 아니다. 어느 티켓에서 온 문장인지는 `git blame`이 그 줄의 커밋 메시지로 데려다준다.

노션은 대상이 아니다. 티켓 추적은 원래 그쪽 몫이다.

적용은 아래 "적용 시점"과 같다. 지금부터 새로 쓰는 것에 적용하고, 기존 표기는 그 파일을 다른 이유로 편집할 때 함께 뺀다.

### 적용 시점

지금부터 새로 쓰거나 고치는 코드에 적용한다. 기존 주석은 소급해서 지우지 않고, 그 파일을 다른 이유로 편집할 때 함께 정리한다. `backend/src/main`만 해도 3479줄 중 1187줄이 주석이라 한 번에 걷어내면 리뷰가 불가능하다.

**주변 코드에 주석이 많아도 그것을 근거로 삼지 않는다.** 기존 파일의 주석 밀도는 규칙 이전 상태이지 따라야 할 본보기가 아니다.

## 문서의 개수 표현

**옆에 목록이 있으면 그 개수를 문장이나 제목에 적지 않는다.** 항목이 늘거나 줄면 숫자는 조용히 틀린다. 주석과 같은 방식으로 썩는다. 아무것도 실패하지 않고, 직접 세어 보기 전까지 아무도 모른다.

"예외 핸들러 넷"이라는 제목 아래에 핸들러 표가 있다고 하자. 표가 이미 목록인데 제목이 개수를 한 번 더 말한다. 핸들러를 붙이는 사람이 표만 고치고 제목을 지나치면 그때부터 문서가 틀린 말을 한다. 제목을 "예외 핸들러"로 두면 표가 곧 목록이라 틀릴 자리가 없다.

| 자리 | 이렇게 쓰지 않는다 | 이렇게 쓴다 |
| --- | --- | --- |
| 표나 목록의 제목 | 예외 핸들러 넷 | 예외 핸들러 |
| 목록을 여는 문장 | 아래 셋 중 하나일 때 쓴다 | 아래 중 하나일 때 쓴다 |
| 목록을 닫는 문장 | 빼고 세는 것은 둘뿐이다 | 빼고 세는 것은 아래뿐이다 |
| 한 문장 안에 나열할 때 | `backend`, `frontend` 두 job이다 | `backend`, `frontend` job이다 |

### 숫자가 내용인 자리

세는 것 자체가 정보면 그대로 쓴다. 목록이 옆에 없어 숫자가 유일한 근거이거나, 값이 바뀌는 것이 곧 뜻이 바뀌는 것일 때다.

- 우리가 센 값: "프레임워크가 내는 예외 20종", "3479줄 중 1187줄이 주석이다"
- 닫혀 있다는 주장: "인터넷에 열린 포트는 80과 443뿐이다"
- 규칙에 박힌 값: "상한은 300줄이다", "본인 외 1명 이상 승인"

가르는 기준은 같은 자리에 목록이 있느냐다. 있으면 목록이 세게 두고, 없으면 숫자를 쓴다.

### 적용 시점

지금부터 새로 쓰거나 고치는 문서에 적용한다. 기존 표현을 한꺼번에 걷어내지 않고, 그 문단을 다른 이유로 편집할 때 함께 고친다.

## README 점검

README는 슬라이스가 끝날 때마다 조금씩 밀린다. "아직 없다"고 적힌 것이 이미 있고, "그 이슈에서 정한다"고 가리킨 이슈가 닫혀 있는 식이다. 한 번에 몰아서 고치지 않으려면 PR을 올리기 전에 아래를 본다. 자기 변경이 해당하는 줄만 보면 된다.

| 이번 PR에서 한 일 | 같이 볼 곳 |
| --- | --- |
| 프론트 화면을 추가하거나 자리표시자를 실제 화면으로 바꿨다 | `frontend/README.md`의 "화면" 표 |
| 마이그레이션이나 시드를 추가/변경했다 | `backend/README.md` 기술 스택의 Flyway 항목 |
| 컨트롤러나 DTO를 건드렸다 | `backend/README.md` "API 문서"의 오퍼레이션 표. 생성물 `backend/openapi.yaml`은 CLAUDE.md의 규칙을 따른다 |
| 오류 code를 추가/변경했거나 오류 응답 형식을 건드렸다 | `backend/README.md` "오류 응답"의 code 목록. 값의 정본은 `ErrorCodes`이고, 프론트엔드는 모르는 code에 기본 문구를 쓴다 |
| 실행 명령이나 사전 준비(런타임 버전, 컨테이너)가 바뀌었다 | 루트 README "시작하기"와 해당 하위 README의 "실행" |
| 배포 구성이나 절차를 바꿨다 (compose, Caddy, 배포 스크립트, CD 워크플로) | `deploy/README.md`. 서버에서 도는 것이 바뀌면 "구성"도 같이 본다 |
| 가리키던 Jira 이슈가 닫혔다 (완료든 취소든) | 그 이슈 키가 적힌 README 줄. 결정이 났으면 결정 내용을 쓰고, 안 났으면 이슈 키를 떼고 미정이라고만 남긴다 |

닫힌 이슈를 가리키는 문장이 제일 오래 남는다. 읽는 사람이 그 이슈를 열어 봐도 답이 없어서다.
