# {도메인} 정책

<!-- 도메인당 파일 하나. 이름은 백엔드 패키지명(customer, event, metric, pricing, invoice, payment).
값은 정책 파일이 정하고 코드가 적용한다. 둘이 다르면 둘 중 하나가 버그다.
주제(예: invoice의 확정, 정정)마다 절 하나. 코드 위치는 grep되는 이름이면 된다.
클래스.상수, 메서드, 테스트 이름, DB 제약 이름, openapi.yaml 스키마 이름.
변경 이력은 두지 않는다. git log가 그 역할이다. -->

## {주제 1. 예: 확정}

| 항목 | 값 | 코드 위치 | 근거 |
|---|---|---|---|
| {항목. 예: 확정 대기 기간} | {값} | `{클래스.상수}` | {RFC-NNN, PR 번호, 또는 초기 관례} |
| {항목} | {값} | `{메서드 또는 테스트 이름}` | {근거} |

왜 {값}: {한 줄 이유. 재검토 조건이 있으면 같이}

<!-- 주제가 하나면 이 절을 지우고, 셋 이상이면 같은 꼴로 더한다. -->
## {주제 2. 예: 정정}

| 항목 | 값 | 코드 위치 | 근거 |
|---|---|---|---|
| {항목} | {값} | `{DB 제약 이름 또는 openapi.yaml 스키마 이름}` | {근거} |

왜 {값}: {한 줄 이유}
