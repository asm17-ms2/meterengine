# 결정과 규칙의 위치와 개정

## 정본

- 이력이 남아야 하는 결정과 규칙은 레포, 흘러가도 되는 것은 Miro다.
- 표에 없는 문서는 만들기 전에 어디에 둘지 먼저 합의한다.

| 종류 | 정본 |
| --- | --- |
| 되돌리기 비싼 방향 결정 | `docs/rfcs/` (쓰는 법은 그 README) |
| 방향 안의 값(정책) | `docs/policies/` (도메인당 파일 하나, 쓰는 법은 그 README) |
| 스프린트 범위 | Jira(MS2 프로젝트)의 스프린트 |
| 회의록, 일정, 브레인스토밍 초안 | Miro |
| API 계약 | `backend/openapi.yaml` (컨트롤러와 DTO에서 자동 생성) |
| 팀 규칙 | `CONTRIBUTING.md`(입구)와 `docs/contributing/` (주제당 파일 하나) |
| 결정과 규칙을 어디에 두고 어떻게 바꾸나 | 이 파일 |
| 브랜치, 커밋, 머지 방식, 릴리스 | `git-workflow.md` |
| 머지 조건, PR 크기, 쪼개기, 스택, 리뷰 | `pull-request.md` |
| 문서 공통 규칙, 시간이 지나면 틀리는 값, README 점검 | `documents.md` |
| 이름 규칙 | `naming.md` |
| 오류 code, 문구, 예외와 핸들러의 자리 | `error-handling.md` |
| 주석과 javadoc | `comments.md` |
| 각 디렉터리의 실행법과 구조 | 그 디렉터리의 `README.md` |

## 가르는 기준

- 먼저 누가 겪는지 묻는다. 도입사면 정책, 외부 계약, RFC. 팀원이면 규칙. 서버면 시스템 구성.
- 그 다음 되돌리면 무엇이 같이 움직이는지로 가른다.

| 질문 | 예이면 |
|---|---|
| 되돌리면 외부 계약(`openapi.yaml`에 나타나는 것)이 같이 바뀌거나 이미 쌓인 데이터를 다시 써야 하는가? | RFC |
| 되돌리면 `build.gradle`의 의존성이 바뀌거나 도메인 패키지 전부가 같은 방향으로 고쳐지는가? (도구가 대신 하는 기계적 변경은 제외) | RFC |
| 그 위에 서는 코드가 없고 리팩토링 PR 하나로 되돌아가는 설계인가? | proposal PR |
| 코드, 문서, 브랜치, PR을 쓰는 방법인가? 되돌리면 파일 한 줄인가? | 팀 규칙 |
| 값 하나(숫자, 길이, 기간, 켜고 끄기)만 바뀌고, 값을 되돌리면 그대로 돌아오나? | 정책 |
| 둘 다 아니고 구현 세부인가? | 코드 |
| 정하는 것이 아니라 하는 일인가? | Jira |

| 예 | 되돌리면 움직이는 것 | 위치 |
|---|---|---|
| 이벤트 스키마의 모양, 오류 응답의 모양, id 타입, 금액 정밀도 | 도입사가 보내는 것, 프론트 계약, 쌓인 행 | RFC |
| 새 엔드포인트, 새 오류 코드, 새 테이블 | 모양을 채우는 것이라 아무것도 | 일반 PR |
| JPA 대신 jOOQ, JUnit 대신 TestNG, 도메인 로직이 사는 층 | 그 위에 선 코드 전체 | RFC |
| 예외 핸들러를 글로벌로 둘지 도메인별로 둘지, 예외 클래스 계층 | 핸들러만. 리팩토링 PR로 되돌아간다 | proposal PR |
| 이름 규칙 표의 한 행, 주석 규칙, PR 상한 300줄 | 파일 한 줄 | 팀 규칙 |
| 인보이스 확정 대기 기간 며칠 | 상수 하나 | 정책 |
| 주석 재고를 전수 정리한다 | 작업이지 결정이 아님 | Jira 스토리 |
| Zoom 대신 Webex | 아무것도 | 슬랙에서 정하고 끝 |

- RFC 하나는 결정 하나다. 따로 바꿀 수 있는 항목은 규칙 파일로 뺀다.
- 애매하면 규칙이나 정책 PR로 올리고, 리뷰에서 "RFC급" 지적이 나오면 RFC로 승격한다.

## 층마다 바꾸는 방법

| 층 | 무엇 | 어디 | 바꾸는 방법 |
|---|---|---|---|
| RFC | 되돌리면 외부 계약, 쌓인 데이터, 그 위에 선 코드 전체가 움직이는 방향 결정 | `docs/rfcs/` | RFC PR. 정족수는 작성자를 뺀 전원. 머지된 본문은 고치지 않는다(오탈자, 죽은 링크 제외) |
| 팀 규칙 | 코드, 문서, 브랜치, 커밋, PR을 쓰는 법 | `CONTRIBUTING.md`와 `docs/contributing/` | 그 파일을 고치는 PR. 정족수는 아래 "규칙 개정" |
| 정책 | 방향 안의 값. 도입사가 겪는 값 | `docs/policies/` | 그 값을 쓰는 코드와 같은 PR |
| 외부 계약 | 도입사와 프론트엔드가 의존하는 응답 필드, 오류 `code`, 상태 코드, 이벤트 필드 | `backend/openapi.yaml`의 description | 모양을 바꾸면 RFC. 채우는 것(새 엔드포인트, 새 오류 코드)은 일반 PR |
| 시스템 구성 | 서버, 배포, 포트, 설정값 | 그 디렉터리 README와 설정 파일 | 설정 PR |
| 코드 | 구현과 실제 적용값 | 소스와 테스트 | 일반 개발. 리팩토링 PR 하나로 되돌아가는 설계 합의는 `proposal` PR |
| 작업 | 범위와 진행 상태 | Jira 티켓 | |

- `docs/`에는 `rfcs/`, `policies/`, `contributing/`만 둔다.
- 첫 도입사 연동 전에는 이미 나간 계약의 모양을 바꾸는 것도 `proposal` PR이다. 연동이 시작되면 RFC다.
  - 연동 뒤 도입사에게 약속하는 유예 기간과 버전 방식은 정책이 정한다.

## 노션

- 노션은 쓰지 않는다. 무료 플랜으로 내려 수정하지 않는 채로 얼린다.
- 남은 내용은 옮기지 않는다. 작업하다 정책이나 RFC가 필요해지면 그때 새로 쓴다.

## 토의 위치

| 단계 | 위치 |
|---|---|
| 초안 전. "이런 규칙이 필요한가", 작업 범위 합의, 찬반 | GitHub Discussions. 댓글 추천으로 가르고, 정식 집계가 필요하면 투표 카테고리 |
| 초안이 생긴 뒤 | 그 PR의 줄 댓글. 본문에 Discussion 링크를 단다 |
| 결론 | 규칙이면 파일 머지, 작업이면 Jira 티켓 설명에 결론과 링크 |
| 알림 | 슬랙. 토의는 하지 않고 링크만 |

- Discussion은 정본이 아니다. 결론은 PR이나 티켓으로 박힌다.

## 제안

- 방향 결정은 RFC PR. 쓰는 법은 `docs/rfcs/README.md`.
- 팀 규칙은 규칙 파일을 고치는 PR. 정책은 코드와 같은 PR.
- 합의가 안 된 변경은 라벨 `proposal`을 붙이고 본문 첫 줄에 합의가 필요한 점을 적는다.
  - 내부 설계 합의(핸들러 배치, 레이어 구조, 모듈 분리)는 코드로 보여 주는 `proposal` PR. 필요하면 Discussion을 먼저 연다.
- Draft PR로 열지 않는다.

## 규칙 파일

- `docs/contributing/`에 주제당 파일 하나. 파일명은 영어 소문자와 하이픈.
- 규칙만 둔다. 근거, 검토한 대안, 이력 절, 출처 링크는 두지 않는다. 왜는 커밋 본문과 RFC가 정본이다.
- 양식은 `documents.md`.
- 한시 규칙은 제목 끝에 "(한시 규칙)"을 붙이고 첫 불릿에 끝나는 조건을 적는다.
- 도구가 검사하는 규칙은 도구 이름과 설정 파일 위치만 적는다.
  - 어겨도 아무것도 실패하지 않는 규칙은 문장을 늘리기 전에 도구로 옮길 수 있는지 본다.

## 규칙 개정

- 규칙 파일(`CONTRIBUTING.md`, `CLAUDE.md`, `docs/contributing/`, `docs/README.md`)은 보호 경로라 어떤 변경이든 전원 승인이다. `protected-paths-approval` 검사가 강제한다.
- RFC 파일은 고치지 않는다. 지금 규칙은 규칙 파일이고 RFC는 그때의 기록이다.

| 변경 | 승인 외에 하는 것 |
|---|---|
| 오탈자, 죽은 링크, 예 보충, 표현 | 없다. 티켓 없이 간다 |
| 값이나 행이 바뀐다 (상한 조정, 행 추가, 예외 추가) | 없다 |
| 절이 새로 생기거나 없어진다, 방향이 바뀐다 | 커밋 본문의 왜에 검토한 대안을 적는다 |
| 끝나는 조건이 적힌 한시 규칙을 지운다 | 조건이 충족됐다는 링크(닫힌 스토리, 머지된 PR)를 본문에 적는다 |
| CI에서 실패하게 만드는 검사 도구를 새로 붙인다 | 그 규칙에 도구 이름과 설정 파일 위치를 적는다 |

## RFC를 뒤집을 때

- 옛 파일을 지우지 않고 새 RFC를 쓴다. 새 문서의 `supersedes`에 옛 번호를, 옛 문서에 `status: superseded`와 `superseded-by`를 적는다.
  - 옛 문서 수정은 새 RFC의 PR에서 같이 한다.
- 일부만 뒤집는 일은 생기지 않아야 한다. 생겼다면 새 RFC에 어느 결정을 대체하는지 적고 옛 RFC는 그대로 둔다.
