# Grafana 데이터 소스. UI에서 만들지 않는다. 이 파일이 정본이다.
apiVersion: 1

datasources:
  - name: Prometheus
    # 경보 규칙(alerting/alerting.yml)과 대시보드 JSON이 이 uid를 참조한다. 바꾸면 셋을 같이 바꾼다.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
